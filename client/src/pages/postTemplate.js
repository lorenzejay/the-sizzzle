import React from "react";
import Layout from "../components/layout";

const PostTemplate = ({ title, id, uploadById, description, cloudinaryId, imageUrl }) => {
  return (
    <Layout>
      <h1>this is a post</h1>
    </Layout>
  );
};

export default PostTemplate;
